# Local-only: publishes convex-dashboard to the host so it's reachable at
# http://localhost:6791 during local development. Not meant to be committed —
# production sits behind a reverse proxy instead.
services:
  convex-dashboard:
    ports:
      - "6792:6791"
